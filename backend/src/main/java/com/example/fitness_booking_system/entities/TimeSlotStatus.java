package com.example.fitness_booking_system.entities;

/**
 * Enum representing the status of a time slot in the fitness booking system.
 */
public enum TimeSlotStatus {
    AVAILABLE,
    BOOKED,
    CANCELLED
}