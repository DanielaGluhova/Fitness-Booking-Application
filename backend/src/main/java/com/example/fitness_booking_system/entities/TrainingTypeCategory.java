package com.example.fitness_booking_system.entities;

/**
 * Enum representing the category of training types in the fitness booking system.
 */
public enum TrainingTypeCategory {
    PERSONAL,
    GROUP
}